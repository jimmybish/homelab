apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: {{ grafana_loki_uid }}
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: true

  - name: Prometheus
    type: prometheus
    uid: {{ grafana_prometheus_uid }}
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: InfluxDB
    type: influxdb
    uid: {{ grafana_influxdb_uid }}
    access: proxy
    url: http://influxdb:8086
    isDefault: false
    editable: true
    jsonData:
      version: Flux
      organization: {{ grafana_influxdb_org }}
      defaultBucket: {{ grafana_influxdb_bucket }}
    secureJsonData:
      token: "{{ grafana_influxdb_admin_token }}"

  - name: InfluxDB - Proxmox
    type: influxdb
    uid: {{ grafana_influxdb_proxmox_uid }}
    access: proxy
    url: http://influxdb:8086
    isDefault: false
    editable: true
    jsonData:
      version: Flux
      organization: {{ grafana_influxdb_proxmox_org }}
      defaultBucket: {{ grafana_influxdb_proxmox_bucket }}
    secureJsonData:
      token: "{{ grafana_influxdb_admin_token }}"
